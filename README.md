# Ебейший проект на Django 

## Инструкция для установки епта
```bash
python -m venv venv
source venv/bin/activate # Активация для Mac Os
.\venv\Scripts\activate # Активация на Windows

pip install -r requirements.txt
```